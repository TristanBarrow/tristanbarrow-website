import React from 'react';
import { Switch } from 'react-router-dom';

const ProjectsPage = () => {
    return (
        <Switch>
            <ProjectsPage />
        </Switch>
    );
}

export default ProjectsPage;